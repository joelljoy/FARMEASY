spring:
  application:
    name: frameasy-backend
  cloud:
    gcp:
      sql:
        database-name: farmeasy
        instance-connection-name: farmeasy-495005:us-central1:farmeasy-db-instance
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql:///farmeasy?cloudSqlInstance=farmeasy-495005:us-central1:farmeasy-db-instance&socketFactory=com.google.cloud.sql.mysql.SocketFactory}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:Samruddhi@2006}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: krushi.app25@gmail.com
    password: kfshcgrgdqiaance
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

# JWT
jwt:
  secret: ${JWT_SECRET:FrameasySuperSecretKeyForJWTTokenGeneration2024Production}
  expiration-ms: 86400000  # 24 hours

# data.gov.in API (schemes)
schemes:
  api-url: https://api.data.gov.in/resource
  cache-duration-hours: 24

# File storage
upload:
  dir: ./uploads
  equipment: equipment
  land: land
  trade: trade

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    com.frameasy: DEBUG
    org.springframework.security: INFO
